https://tailwindcss.com/docs/installation

How it works
    Tailwind scans the 
        HTML files
        JavaScript components
        and any other templates
            for
                class names
                generating the corresponding styles and then
                    writing them to a static CSS file